pre-commit:
  commands:
    ruff-check:
      glob: "*.py" 
      stage_fixed: true
      run: uv run ruff check {staged_files} --fix --exit-non-zero-on-fix 
    ruff-fmt:
      glob: "*.py" 
      stage_fixed: true
      run: uv run ruff format {staged_files}