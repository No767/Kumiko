version: "3.9"
services:
  postgres:
    container_name: Kumiko-Postgres-Dev
    image: no767/kumiko-postgres:edge
    restart: always
    env_file:
      - .env
    volumes:
      - dev_postgres_volume:/var/lib/postgresql/data
    ports:
      - 5432:5432

  mongodb:
    container_name: Kumiko-MongoDB-Dev
    image: mongo:6
    restart: always
    env_file:
      - .env
    ports:
      - 27017:27017
    volumes:
      - dev_mongodb_volume:/data/db


  redis:
    container_name: Kumiko-Redis-Dev
    image: redis:7
    ports:
      - 6379:6379
    volumes:
      - dev_redis_volume:/data
    command: redis-server --save 60 1 --loglevel warning

volumes:
  dev_postgres_volume:
  dev_mongodb_volume:
  dev_redis_volume:
