import bs4
import discord
from discord.ext import commands, Embed
import 
class pixiv_scrapper:
    def __init__(self, bot):
        self.bot = bot
        
    @commands.command(name-'pixiv')
    async def on_message(self, ctx, search:str):
        
